# Required in production. Use a long random string (e.g. 32+ chars).
SESSION_SECRET=your-secret-here

# Frontend URL for CORS. In production set to your app URL (e.g. https://yourapp.com).
CORS_ORIGIN=http://localhost:5173

# Optional. Bcrypt cost (10–12). Higher = slower and stronger. Default 10.
# BCRYPT_ROUNDS=10

# Optional. Server port. Default 3001.
# PORT=3001
